@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">Price {{ $price->price_id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('admin/prices/' . $price->price_id . '/edit') }}" class="btn btn-primary btn-xs" title="Edit Price"><span class="glyphicon glyphicon-pencil" aria-hidden="true"/></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/prices', $price->price_id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<span class="glyphicon glyphicon-trash" aria-hidden="true"/>', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete Price',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $price->price_id }}</td>
                                    </tr>
                                    <tr><th> MotifId </th><td> {{ $price->motifId }} </td></tr><tr><th> SizeId </th><td> {{ $price->sizeId }} </td></tr><tr><th> ColorId </th><td> {{ $price->colorId }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection